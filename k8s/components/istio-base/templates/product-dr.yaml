apiVersion: networking.istio.io/v1beta1
kind: DestinationRule
metadata:
  name: product-dr
spec:
  host: product
  subsets:
    - labels:
        version: v1
      name: old
    - labels:
        version: v2
      name: new
  trafficPolicy:
    tls:
      mode: ISTIO_MUTUAL